fastapi==0.138.0
starlette==1.3.1
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.32
pypdf==6.14.2
pdfplumber==0.11.10
pdfminer.six==20260107
httpx==0.27.2
python-dateutil==2.9.0
passlib==1.7.4
bcrypt==4.2.1
itsdangerous==2.2.0
apscheduler==3.10.4

# 测试
pytest==9.0.3

# OCR 兜底（可选）：扫描件 PDF 需要安装以下依赖及系统工具。
# 完整 OCR 环境请使用 requirements-ocr.txt + Dockerfile，或运行：
#   docker compose -f backend/docker-compose.ocr-test.yml up --build
# pdf2image 需要系统安装 poppler（Windows: poppler for windows / Linux: poppler-utils）
# paddleocr 为中文 OCR 首选，体积较大；tesseract 为轻量回退
